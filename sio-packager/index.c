/* 
#include "index.h"

void create_index (Index *index)
{
	index = malloc (sizeof(Index));
	index.size = 0;
	index.resources = malloc (sizeof(Resource) * 0);
}

void track_resource ( Index * index, unsigned char *path )
{

}*/